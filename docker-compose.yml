#docker run --name api-solid-pg -e POSTGRESQL_USERNAME=docker -e POSTGRESQL_PASSWORD=docker 
#-e POSTGRESQL_DATABASE=apisolid -p 5432:5432 bitnami/postgresql

# docker compose up -d Executa o arquivo docker compose
# docker compose storp Para o container 
# docker compose down Deleta todos os containers e dados

version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=docker
      - POSTGRESQL_PASSWORD=docker
      - POSTGRESQL_DATABASE=apisolid